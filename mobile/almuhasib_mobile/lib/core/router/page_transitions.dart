import 'package:get/get.dart';

const fadeSlideTransition = Transition.fadeIn;
const defaultTransitionDuration = Duration(milliseconds: 400);
const slideTransition = Transition.rightToLeftWithFade;
const slideTransitionDuration = Duration(milliseconds: 380);
