namespace AlMuhasib.Core.Enums.Gold;

/// <summary>
/// Direction of an invoice line for exchange invoices.
/// In = scrap/old metal incoming (stock+); Out = new metal outgoing (stock-).
/// </summary>
public enum GoldInvoiceLineDirection
{
    In = 0,
    Out = 1
}
