<UserControl x:Class="AlMuhasib.UI.Controls.CustomerQuickStatementOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft"
             Visibility="Collapsed"
             Background="#99000000">
    <Grid>
        <Border MaxWidth="640" MinWidth="400" Margin="24,80,24,24"
                VerticalAlignment="Top" HorizontalAlignment="Center"
                Background="{DynamicResource CardBrush}" CornerRadius="16"
                Padding="24" materialDesign:ElevationAssist.Elevation="Dp12">
            <StackPanel>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding QuickStatementCustomerName}" FontSize="20" FontWeight="Bold"/>
                        <TextBlock Text="{Binding QuickStatementBalance, StringFormat=الرصيد: {0:N0} د.ع}" Margin="0,4,0,0"/>
                        <TextBlock Text="{Binding QuickStatementOverdueText}" Foreground="#C62828" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" Style="{StaticResource IconButton}"
                            Command="{Binding CloseQuickStatementCommand}">
                        <materialDesign:PackIcon Kind="Close"/>
                    </Button>
                </Grid>

                <UniformGrid Columns="3" Margin="0,0,0,16">
                    <Button Command="{Binding PrintQuickStatementCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer"/>
                            <TextBlock Text="طباعة" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportQuickStatementPdfCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilePdfBox"/>
                            <TextBlock Text="PDF/Excel" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ShareQuickStatementWhatsAppCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="4,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Whatsapp"/>
                            <TextBlock Text="واتساب" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                </UniformGrid>

                <DataGrid ItemsSource="{Binding QuickStatementLines}" MaxHeight="300"
                          Style="{StaticResource ModernDataGrid}"
                          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat=yyyy/MM/dd}" Width="90"/>
                        <DataGridTextColumn Header="البيان" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="الرصيد" Binding="{Binding RunningBalance, StringFormat=N0}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
