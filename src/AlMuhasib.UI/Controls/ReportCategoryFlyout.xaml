<UserControl x:Class="AlMuhasib.UI.Controls.ReportCategoryFlyout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:models="clr-namespace:AlMuhasib.UI.Models"
             FlowDirection="RightToLeft"
             Width="300">

    <UserControl.Resources>
        <Style x:Key="ReportCardButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Card"
                                Background="#FAFBFC"
                                CornerRadius="11"
                                BorderBrush="#E8EDF2"
                                BorderThickness="1"
                                Padding="6,7">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border x:Name="IconHost"
                                        Width="44" Height="44"
                                        CornerRadius="11"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,4">
                                    <materialDesign:PackIcon x:Name="IconGlyph"
                                                             Kind="{Binding Icon}"
                                                             Width="24" Height="24"
                                                             HorizontalAlignment="Center"
                                                             VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding Title}"
                                           TextAlignment="Center"
                                           FontSize="11.5"
                                           FontWeight="SemiBold"
                                           Foreground="#455A64"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap"
                                           MaxWidth="120"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="White"/>
                                <Setter TargetName="Card" Property="BorderBrush" Value="#BBDEFB"/>
                                <Setter TargetName="Card" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1" Color="#1565C0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#F1F8FF"/>
                                <Setter TargetName="Card" Property="BorderBrush" Value="#90CAF9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border x:Name="FlyoutRoot"
            Background="{DynamicResource CardBrush}"
            BorderBrush="#E2E8F0"
            BorderThickness="1,0,0,0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="-2" Opacity="0.08" Color="#0D1B2A" Direction="0"/>
        </Border.Effect>
        <Grid VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="HeaderBar" Grid.Row="0" Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="34" Height="34" CornerRadius="9"
                            Background="#33FFFFFF" Margin="0,0,8,0">
                        <materialDesign:PackIcon Kind="{Binding ActiveReportCategoryIcon}"
                                                 Width="19" Height="19"
                                                 Foreground="White"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ActiveReportCategoryTitle}"
                                   FontSize="14" FontWeight="Bold" Foreground="White"/>
                        <TextBlock FontSize="10.5" Foreground="#E3F2FD" Margin="0,1,0,0">
                            <Run Text="{Binding ReportFlyoutItems.Count, Mode=OneWay}"/>
                            <Run Text=" "/>
                            <Run Text="{Binding ActiveFlyoutItemLabel, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1"
                          Style="{StaticResource ThinScrollViewer}"
                          Padding="4,2,4,6"
                          VerticalAlignment="Top"
                          VerticalScrollBarVisibility="Auto"
                          MaxHeight="480">
                <ItemsControl ItemsSource="{Binding ReportFlyoutItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel ItemWidth="144" ItemHeight="84"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ReportMenuEntry}">
                            <Button Style="{StaticResource ReportCardButton}"
                                    Click="ReportCard_Click"
                                    Loaded="ReportCard_Loaded"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
