<UserControl x:Class="AlMuhasib.UI.Controls.HotelListStatsBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:AlMuhasib.UI.Models"
             FlowDirection="RightToLeft"
             x:Name="Root">
    <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=Root}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate DataType="{x:Type models:HotelListStatItem}">
                <Border Margin="0,0,10,8" Padding="14,10" CornerRadius="10"
                        Background="#F5F7FA" BorderBrush="#E0E6ED" BorderThickness="1" MinWidth="110">
                    <StackPanel>
                        <TextBlock Text="{Binding Label}" FontSize="11" Foreground="#78909C"/>
                        <TextBlock Text="{Binding Value}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0"
                                   Foreground="{Binding AccentColor, Converter={StaticResource HexToBrushConverter}}"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
