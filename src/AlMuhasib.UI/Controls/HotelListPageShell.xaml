<UserControl x:Class="AlMuhasib.UI.Controls.HotelListPageShell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:AlMuhasib.UI.Controls"
             x:Name="Root"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <md:Card Grid.Row="0" Style="{StaticResource ElevatedCard}" Padding="20" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{Binding PageTitle, ElementName=Root}"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"
                           Margin="0,0,0,12"/>
                <ContentPresenter Content="{Binding ToolbarContent, ElementName=Root}"/>
            </StackPanel>
        </md:Card>

        <!-- Main list + preview -->
        <md:Card Grid.Row="1" Style="{StaticResource ElevatedCard}" Padding="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="{Binding PreviewPanelWidth, ElementName=Root}"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="16,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding ListTitle, ElementName=Root}"
                                       FontSize="15" FontWeight="Bold" Foreground="White"
                                       VerticalAlignment="Center"/>
                            <ContentPresenter Grid.Column="1"
                                              Content="{Binding ListHeaderExtra, ElementName=Root}"
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <controls:HotelListStatsBar Grid.Row="1"
                                                Margin="16,12,16,4"
                                                ItemsSource="{Binding StatsItemsSource, ElementName=Root}"
                                                Visibility="{Binding ShowStatsBar, ElementName=Root, Converter={StaticResource BoolVis}}"/>

                    <ContentPresenter Grid.Row="2"
                                      Content="{Binding ListContent, ElementName=Root}"
                                      Margin="0"/>

                    <ContentPresenter Grid.Row="3"
                                      Content="{Binding ListFooterContent, ElementName=Root}"
                                      Visibility="{Binding ListFooterContent, ElementName=Root, Converter={StaticResource NullToCollapsedConverter}}"/>

                    <controls:PaginationBar Grid.Row="4"
                                            Visibility="{Binding ShowPagination, ElementName=Root, Converter={StaticResource BoolVis}}"/>
                </Grid>

                <controls:HotelEntityPreviewPanel Grid.Column="1"
                                                    HasSelection="{Binding HasPreviewSelection, ElementName=Root}"
                                                    Title="{Binding PreviewTitle, ElementName=Root}"
                                                    Subtitle="{Binding PreviewSubtitle, ElementName=Root}"
                                                    IconKind="{Binding PreviewIconKind, ElementName=Root}"
                                                    SelectedTabIndex="{Binding PreviewSelectedTab, ElementName=Root, Mode=TwoWay}"
                                                    SummaryContent="{Binding PreviewSummaryContent, ElementName=Root}"
                                                    HistoryContent="{Binding PreviewHistoryContent, ElementName=Root}"
                                                    ActionsContent="{Binding PreviewActionsContent, ElementName=Root}"
                                                    CloseCommand="{Binding ClosePreviewCommand, ElementName=Root}"
                                                    ShowCloseButton="{Binding ShowPreviewCloseButton, ElementName=Root}"
                                                    Visibility="{Binding ShowPreviewPanel, ElementName=Root, Converter={StaticResource BoolVis}}"/>
            </Grid>
        </md:Card>
    </Grid>
</UserControl>
