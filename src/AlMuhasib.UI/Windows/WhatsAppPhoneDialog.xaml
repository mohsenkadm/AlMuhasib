<Window x:Class="AlMuhasib.UI.Windows.WhatsAppPhoneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="رقم واتساب"
        Width="420" Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="إرسال عبر واتساب" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" x:Name="SubtitleText" TextWrapping="Wrap" Opacity="0.75" Margin="0,0,0,16"/>

        <TextBox Grid.Row="2"
                 x:Name="PhoneTextBox"
                 materialDesign:HintAssist.Hint="رقم الهاتف (مثال: 07765694495)"
                 materialDesign:HintAssist.IsFloating="True"
                 Style="{StaticResource FilledTextBox}"
                 FontSize="16"
                 VerticalAlignment="Top"
                 KeyDown="PhoneTextBox_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
            <Button Content="إلغاء" IsCancel="True" Style="{StaticResource FlatButton}" Padding="20,8" Margin="0,0,8,0"/>
            <Button Content="متابعة" IsDefault="True" Click="Ok_Click" Style="{StaticResource PrimaryButton}" Padding="24,8"/>
        </StackPanel>
    </Grid>
</Window>
