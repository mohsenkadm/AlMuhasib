<UserControl x:Class="AlMuhasib.UI.Views.Gold.GoldNotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:AlMuhasib.UI.Controls"
             Style="{StaticResource PageStyle}"
             FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,16">
            <TextBlock Text="التنبيهات" FontSize="24" FontWeight="Bold" Foreground="#B8860B" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Border Background="#FFF3E0" CornerRadius="10" Padding="12,6" Margin="0,0,12,0" VerticalAlignment="Center">
                    <TextBlock>
                        <Run Text="غير مقروء:"/><Run Text=" "/><Run Text="{Binding UnreadCount}" FontWeight="Bold"/>
                    </TextBlock>
                </Border>
                <Button Command="{Binding RefreshGenerateCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Content="تحديث / توليد"/>
                <Button Command="{Binding MarkAllReadCommand}" Style="{StaticResource OutlinedButton}" Margin="0,0,8,0" Content="تعليم الكل كمقروء"/>
                <Button Command="{Binding LoadCommand}" Style="{StaticResource FlatButton}">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </StackPanel>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <CheckBox Content="غير المقروء فقط" IsChecked="{Binding UnreadOnly}" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Message}" Foreground="#2E7D32" Margin="16,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C62828" Margin="16,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="2" Style="{StaticResource ElevatedCard}" Padding="0">
            <DataGrid ItemsSource="{Binding Alerts}" SelectedItem="{Binding SelectedAlert}"
                      AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource ModernDataGrid}" Margin="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="النوع" Binding="{Binding Type}" Width="120"/>
                    <DataGridTextColumn Header="العنوان" Binding="{Binding Title}" Width="180"/>
                    <DataGridTextColumn Header="الرسالة" Binding="{Binding Message}" Width="*"/>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAt, StringFormat=yyyy/MM/dd HH:mm}" Width="140"/>
                    <DataGridCheckBoxColumn Header="مقروء" Binding="{Binding IsRead}" Width="70"/>
                    <DataGridTemplateColumn Header="" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="مقروء" Style="{StaticResource FlatButton}"
                                        Command="{Binding DataContext.MarkReadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <controls:PaginationBar Grid.Row="3" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
