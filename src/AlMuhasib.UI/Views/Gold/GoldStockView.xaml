<UserControl x:Class="AlMuhasib.UI.Views.Gold.GoldStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:AlMuhasib.UI.Controls"
             Style="{StaticResource PageStyle}"
             FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <md:Card Grid.Row="0" Padding="18" Margin="0,0,0,14" UniformCornerRadius="14"
                 BorderBrush="#E8D5A3" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="مخزون الذهب" FontSize="20" FontWeight="Bold" Foreground="#B8860B"/>
                    <TextBlock Text="أرصدة العيارات مع تمييز المخزون المنخفض" FontSize="12"
                               Foreground="#8D6E63" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding OpenAdjustmentCommand}" Style="{StaticResource PrimaryButton}"
                            Height="36" Padding="14,0" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="TuneVerticalVariant" Width="18" Height="18" VerticalAlignment="Center"/>
                            <TextBlock Text="تسوية" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}"
                            Width="36" Height="36" ToolTip="تحديث">
                        <md:PackIcon Kind="Refresh" Width="20" Height="20"/>
                    </Button>
                </StackPanel>
            </Grid>
        </md:Card>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,14">
            <md:Card Padding="16" Margin="0,0,10,0" UniformCornerRadius="12" BorderBrush="#E8D5A3" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="إجمالي الوزن" Foreground="#8D6E63" FontSize="12"/>
                    <TextBlock Text="{Binding TotalGrams, StringFormat={}{0:N2} غ}" FontSize="22" FontWeight="Bold" Foreground="#B8860B"/>
                </StackPanel>
            </md:Card>
            <md:Card Padding="16" Margin="0,0,10,0" UniformCornerRadius="12" BorderBrush="#E8D5A3" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="قيمة المخزون" Foreground="#8D6E63" FontSize="12"/>
                    <TextBlock Text="{Binding TotalValue, StringFormat={}{0:N0} د.ع}" FontSize="22" FontWeight="Bold" Foreground="#8B6914"/>
                </StackPanel>
            </md:Card>
            <md:Card Padding="16" UniformCornerRadius="12" BorderBrush="#FFCDD2" BorderThickness="1" Background="#FFEBEE">
                <StackPanel>
                    <TextBlock Text="عيارات منخفضة" Foreground="#C62828" FontSize="12"/>
                    <TextBlock Text="{Binding LowStockCount}" FontSize="22" FontWeight="Bold" Foreground="#C62828"/>
                </StackPanel>
            </md:Card>
        </UniformGrid>

        <md:Card Grid.Row="2" Padding="0" UniformCornerRadius="14" BorderBrush="#E8D5A3" BorderThickness="1">
            <DockPanel>
                <controls:ListTableHeaderBar DockPanel.Dock="Top" Title="أرصدة المخزون حسب العيار"
                                             ShowFilter="False" ShowExport="False" ShowPrint="False" ShowCardToggle="False"/>
                <DataGrid ItemsSource="{Binding StockRows}"
                          SelectedItem="{Binding SelectedRow}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" HeadersVisibility="Column"
                          Style="{StaticResource ModernDataGrid}" Margin="8">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                    <Setter Property="Background" Value="#FFEBEE"/>
                                    <Setter Property="Foreground" Value="#B71C1C"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="العيار" Binding="{Binding KaratName}" Width="120"/>
                        <DataGridTextColumn Header="القيمة" Binding="{Binding KaratValue}" Width="70"/>
                        <DataGridTextColumn Header="الوزن (غ)" Binding="{Binding GramsOnHand, StringFormat=N2}" Width="110"/>
                        <DataGridTextColumn Header="متوسط التكلفة/غ" Binding="{Binding AverageCostPerGram, StringFormat=N0}" Width="130"/>
                        <DataGridTextColumn Header="قيمة المخزون" Binding="{Binding StockValue, StringFormat=N0}" Width="130"/>
                        <DataGridTextColumn Header="عدد القطع" Binding="{Binding PieceCount}" Width="90"/>
                        <DataGridTemplateColumn Header="تنبيه" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <md:PackIcon Kind="Alert" Width="18" Height="18" Foreground="#C62828">
                                            <md:PackIcon.Style>
                                                <Style TargetType="md:PackIcon">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </md:PackIcon.Style>
                                        </md:PackIcon>
                                        <TextBlock Text="منخفض" Foreground="#C62828" Margin="4,0,0,0" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </md:Card>
    </Grid>
</UserControl>
