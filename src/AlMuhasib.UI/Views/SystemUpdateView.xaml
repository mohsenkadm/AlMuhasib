<UserControl x:Class="AlMuhasib.UI.Views.SystemUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:AlMuhasib.UI.ViewModels"
             Style="{StaticResource PageStyle}"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="8" MaxWidth="720" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource GradientIconCircle}" Margin="0,0,16,0">
                        <materialDesign:PackIcon Kind="CloudDownload" Width="28" Height="28"
                                                 Foreground="White"
                                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="تحديث النظام"
                                   FontSize="{StaticResource HugeFontSize}"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryHueDarkBrush}"/>
                        <TextBlock Text="تحقق يدوياً من وجود إصدار جديد — الفحص التلقائي عند التشغيل يبقى كما هو"
                                   FontSize="{StaticResource NormalFontSize}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <materialDesign:Card Grid.Row="1" UniformCornerRadius="14" Padding="24,20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="الإصدار الحالي" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding VersionLabel}" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryHueDarkBrush}" Margin="0,4,0,20"/>

                    <Border Background="#F5F7FA" CornerRadius="10" Padding="14,12" Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <materialDesign:PackIcon Kind="Update" Width="18" Height="18"
                                                         Foreground="#1565C0" VerticalAlignment="Center"/>
                                <TextBlock Text="الفحص التلقائي" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                                <Run Text="عند فتح البرنامج يتم التحقق تلقائياً من التحديثات إذا كان الإنترنت متاحاً."/>
                                <LineBreak/>
                                <Run Text="يمكنك استخدام الزر أدناه للتحقق فوراً دون انتظار الفحص التلقائي."/>
                            </TextBlock>
                            <TextBlock Margin="0,10,0,0" FontSize="11" FontWeight="Medium" Foreground="#2E7D32">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="● الفحص التلقائي عند التشغيل: معطّل"/>
                                        <Setter Property="Foreground" Value="#C62828"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AutoCheckOnStartup}" Value="True">
                                                <Setter Property="Text" Value="● الفحص التلقائي عند التشغيل: مفعّل"/>
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <Button Command="{Binding CheckForUpdatesCommand}"
                            Style="{StaticResource AccentButton}"
                            Height="44" Padding="24,0"
                            IsEnabled="{Binding IsChecking, Converter={StaticResource InverseBooleanConverter}}"
                            materialDesign:ButtonAssist.CornerRadius="10"
                            HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CloudSync" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="التحقق من التحديثات الآن" Margin="8,0,0,0"
                                       VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>

                    <StackPanel Orientation="Horizontal" Margin="0,14,0,0"
                                Visibility="{Binding IsChecking, Converter={StaticResource BoolVis}}">
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True" Width="22" Height="22" Margin="0,0,10,0"/>
                        <TextBlock Text="جاري التحقق من الخادم..." VerticalAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="2" UniformCornerRadius="14" Padding="18,14"
                                 Visibility="{Binding UpdatesEnabled, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="20" Height="20"
                                             Foreground="#EF6C00" VerticalAlignment="Top" Margin="0,2,10,0"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                               Text="التحديث عبر الإنترنت غير مفعّل في إعدادات هذا الإصدار. تواصل مع الدعم للحصول على التحديثات يدوياً."/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </ScrollViewer>
</UserControl>
