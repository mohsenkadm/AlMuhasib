<UserControl x:Class="AlMuhasib.UI.Views.RealEstate.RealEstatePartiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:AlMuhasib.UI.Controls"
             Style="{StaticResource PageStyle}" FlowDirection="RightToLeft">
  <Grid Margin="16">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <DockPanel Grid.Row="0" Margin="0,0,0,12">
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
        <Button Content="زبون جديد" Command="{Binding OpenNewCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0"/>
        <TextBox Width="220" md:HintAssist.Hint="بحث" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
      </StackPanel>
      <TextBlock Text="الزبائن" FontSize="20" FontWeight="Bold" Foreground="#1565C0"/>
    </DockPanel>
    <DataGrid Grid.Row="1" ItemsSource="{Binding Parties}" SelectedItem="{Binding SelectedParty}" AutoGenerateColumns="False" IsReadOnly="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*"/>
        <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="120"/>
        <DataGridTextColumn Header="العنوان" Binding="{Binding Address}" Width="*"/>
        <DataGridTextColumn Header="الهوية" Binding="{Binding IdNumber}" Width="120"/>
        <DataGridTextColumn Header="الدين" Binding="{Binding TotalDebt, StringFormat=N0}" Width="100"/>
        <DataGridTemplateColumn Header="إجراءات" Width="160">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal">
                <Button Content="تعديل" Command="{Binding DataContext.OpenEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,4,0"/>
                <Button Content="حذف" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
              </StackPanel>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
    <controls:PaginationBar Grid.Row="2" Margin="0,12,0,0"/>
    <md:DialogHost IsOpen="{Binding IsEditDialogOpen}" Grid.RowSpan="3">
      <md:DialogHost.DialogContent>
        <StackPanel Margin="24" Width="360">
          <TextBlock Text="بيانات الزبون" FontWeight="Bold" Margin="0,0,0,12"/>
          <TextBox md:HintAssist.Hint="الاسم" Text="{Binding EditName}" Margin="0,0,0,8"/>
          <TextBox md:HintAssist.Hint="الهاتف" Text="{Binding EditPhone}" Margin="0,0,0,8"/>
          <TextBox md:HintAssist.Hint="العنوان" Text="{Binding EditAddress}" Margin="0,0,0,8"/>
          <TextBox md:HintAssist.Hint="الهوية" Text="{Binding EditIdNumber}" Margin="0,0,0,12"/>
          <StackPanel Orientation="Horizontal">
            <Button Content="حفظ" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0"/>
            <Button Content="إلغاء" Command="{Binding CloseEditCommand}"/>
          </StackPanel>
        </StackPanel>
      </md:DialogHost.DialogContent>
    </md:DialogHost>
  </Grid>
</UserControl>
