<UserControl x:Class="AlMuhasib.UI.Views.Hotel.RestaurantKitchenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:AlMuhasib.UI.Controls"
             Style="{StaticResource PageStyle}"
             FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <md:Card Style="{StaticResource ElevatedCard}" Padding="20" Margin="0,0,0,16" md:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="شاشة المطبخ (KDS)" FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,12"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button Command="{Binding LoadOrdersCommand}" Style="{StaticResource FlatButton}" md:ButtonAssist.CornerRadius="8" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="تحديث"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding MarkPreparingCommand}" Style="{StaticResource OutlinedButton}"
                            md:ButtonAssist.CornerRadius="8" Padding="16,8" Margin="0,0,8,0"
                            IsEnabled="{Binding HasSelectedOrder}">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="PotSteam" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="قيد التحضير"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding MarkReadyCommand}" Style="{StaticResource PrimaryButton}"
                            md:ButtonAssist.CornerRadius="8" Padding="16,8" Margin="0,0,8,0"
                            IsEnabled="{Binding HasSelectedOrder}">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="CheckCircle" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="جاهز"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding MarkServedCommand}" Style="{StaticResource OutlinedButton}"
                            md:ButtonAssist.CornerRadius="8" Padding="16,8"
                            IsEnabled="{Binding HasSelectedOrder}">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="RoomService" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="تم التقديم"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </md:Card>

        <controls:HotelListStatsBar Grid.Row="1" Margin="0,0,0,16" ItemsSource="{Binding Stats}"/>

        <md:Card Grid.Row="2" Style="{StaticResource ElevatedCard}" Padding="12" md:ElevationAssist.Elevation="Dp2">
            <ListBox ItemsSource="{Binding KitchenOrders}" SelectedItem="{Binding SelectedOrder}"
                     Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="8"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <md:Card Width="300" Padding="14" UniformCornerRadius="12">
                            <md:Card.Style>
                                <Style TargetType="md:Card" BasedOn="{StaticResource ElevatedCard}">
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </md:Card.Style>
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding OrderNumber}" FontWeight="Bold" FontSize="17"/>
                                    <Border Grid.Column="1" CornerRadius="6" Padding="8,3" Background="{DynamicResource PrimaryHueLightBrush}">
                                        <TextBlock Text="{Binding KitchenStatus, Converter={StaticResource RestaurantEnumLabelConverter}}"
                                                   FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                                <TextBlock Text="{Binding OrderType, Converter={StaticResource RestaurantEnumLabelConverter}}"
                                           Opacity="0.7" FontSize="12" Margin="0,0,0,8"/>
                                <Separator Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding Lines}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,3,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding ItemName}" FontWeight="SemiBold"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat='× {0}'}" Opacity="0.8"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </md:Card>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </md:Card>
    </Grid>
</UserControl>
