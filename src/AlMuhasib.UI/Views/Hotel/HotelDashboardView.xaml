<UserControl x:Class="AlMuhasib.UI.Views.Hotel.HotelDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Style="{StaticResource PageStyle}"
             FlowDirection="RightToLeft">
    <ScrollViewer Padding="24">
        <StackPanel>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="لوحة التحكم — إدارة الفندق" FontSize="20" FontWeight="Bold" Foreground="#6A1B9A"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding OpenCheckInOutCommand}" Style="{StaticResource OutlinedButton}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="Login" Margin="0,0,6,0"/>
                            <TextBlock Text="تسجيل دخول/خروج"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenReservationsCommand}" Style="{StaticResource OutlinedButton}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="CalendarClock" Margin="0,0,6,0"/>
                            <TextBlock Text="الحجوزات"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenNewReservationCommand}" Style="{StaticResource PrimaryButton}" Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="CalendarPlus" Margin="0,0,6,0"/>
                            <TextBlock Text="حجز جديد"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding LoadDataCommand}" Style="{StaticResource FlatButton}" Margin="4,0">
                        <md:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>

            <UniformGrid Columns="4" Margin="0,0,0,16">
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="الغرف المتاحة" Foreground="#78909C"/><TextBlock Text="{Binding AvailableRooms}" FontSize="24" FontWeight="Bold"/></StackPanel>
                </md:Card>
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="مشغولة" Foreground="#78909C"/><TextBlock Text="{Binding OccupiedRooms}" FontSize="24" FontWeight="Bold"/></StackPanel>
                </md:Card>
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="وصول اليوم" Foreground="#78909C"/><TextBlock Text="{Binding TodayArrivalCount}" FontSize="24" FontWeight="Bold"/></StackPanel>
                </md:Card>
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="نسبة الإشغال %" Foreground="#78909C"/><TextBlock Text="{Binding OccupancyRate}" FontSize="24" FontWeight="Bold"/></StackPanel>
                </md:Card>
            </UniformGrid>

            <ItemsControl ItemsSource="{Binding SmartAlerts}" Margin="0,0,0,16">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <md:Card Padding="12" Margin="0,0,0,8" Background="#FFF3E0">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Message}" Foreground="#616161"/>
                            </StackPanel>
                        </md:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <md:Card Grid.Column="0" Style="{StaticResource ElevatedCard}" Padding="12" Margin="4">
                    <StackPanel>
                        <TextBlock Text="اتجاه الإيراد (7 أيام)" FontWeight="Bold" Margin="0,0,0,8"/>
                        <lvc:CartesianChart Series="{Binding RevenueSeries}" XAxes="{Binding RevenueXAxes}" YAxes="{Binding RevenueYAxes}" Height="240"/>
                    </StackPanel>
                </md:Card>
                <md:Card Grid.Column="1" Style="{StaticResource ElevatedCard}" Padding="12" Margin="4">
                    <StackPanel>
                        <TextBlock Text="حالة الغرف" FontWeight="Bold" Margin="0,0,0,8"/>
                        <lvc:PieChart Series="{Binding RoomStatusSeries}" Height="240"/>
                    </StackPanel>
                </md:Card>
            </Grid>

            <UniformGrid Columns="3" Margin="0,0,0,16">
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="إيراد اليوم" Foreground="#78909C"/><TextBlock Text="{Binding TodayRevenue, StringFormat=N0}" FontSize="20" FontWeight="Bold"/></StackPanel>
                </md:Card>
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="إيراد الشهر" Foreground="#78909C"/><TextBlock Text="{Binding MonthRevenue, StringFormat=N0}" FontSize="20" FontWeight="Bold"/></StackPanel>
                </md:Card>
                <md:Card Style="{StaticResource ElevatedCard}" Padding="16" Margin="4">
                    <StackPanel><TextBlock Text="أرصدة مستحقة" Foreground="#78909C"/><TextBlock Text="{Binding OutstandingBalances, StringFormat=N0}" FontSize="20" FontWeight="Bold" Foreground="#C62828"/></StackPanel>
                </md:Card>
            </UniformGrid>

            <md:Card Style="{StaticResource ElevatedCard}" Padding="12" Margin="4">
                <StackPanel>
                    <TextBlock Text="أحدث الحجوزات" FontWeight="Bold" Margin="0,0,0,8"/>
                    <DataGrid ItemsSource="{Binding RecentReservations}" AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="220" Style="{StaticResource HotelListDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم" Binding="{Binding ReservationNumber}" Width="110" MinWidth="90"
                                                ElementStyle="{StaticResource HotelDataGridTextElement}"/>
                            <DataGridTextColumn Header="النزيل" Binding="{Binding GuestName}" Width="2*" MinWidth="140"
                                                ElementStyle="{StaticResource HotelDataGridTextElement}"/>
                            <DataGridTextColumn Header="الغرفة" Binding="{Binding RoomNumber}" Width="90" MinWidth="70"
                                                ElementStyle="{StaticResource HotelDataGridTextElement}"/>
                            <DataGridTextColumn Header="الوصول" Binding="{Binding CheckInDate, StringFormat=yyyy/MM/dd}" Width="110" MinWidth="90"
                                                ElementStyle="{StaticResource HotelDataGridTextElement}"/>
                            <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat=N0}" Width="110" MinWidth="90"
                                                ElementStyle="{StaticResource HotelDataGridTextElement}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </md:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
